import 'package:flutter/material.dart';

class WeeklySteps extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 250,
      color: Colors.green,
      child: Center(child: Text('Sommario settimanale')),
    );
  }
}
